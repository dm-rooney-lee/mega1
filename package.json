{
  "name": "game-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "2D platformer POC built with Phaser 4 + TypeScript + Vite",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "husky"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "typescript": "^5.7.0",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
